# 复制为同目录下的 config.local.yaml（已在 .gitignore，勿提交）。
# 当本机 MySQL/Redis 与 config.yaml 默认值不一致时使用；只需填写与你不一致的字段。

# database:
#   host: "127.0.0.1"
#   port: 3306
#   database: "moment"
#   username: "root"
#   password: "请填写本机 MySQL 密码"

# redis:
#   host: "127.0.0.1"
#   port: 6379
